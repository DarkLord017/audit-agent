"""Per-ecosystem profile modules, auto-imported by profiles.py.

Drop ``<lang>.py`` here and call ``registry.register(...)``. Solidity stays
in profiles.py; this package is for every language that lands after it.
"""
